# 自己產出的

我們寫的、開源的。不是上游換皮。也**不是** AIWFF 工廠（daemon、inbox、排程、state.json）。

學員要跟做，去該 repo 讀它自己的 `AGENTS.md`（沒有就讀 README），不要從本 kit 猜安裝。

| repo | 一句 | 不是什麼 |
|---|---|---|
| [minibrain-cli](https://github.com/zaxardery8011-design/minibrain-cli) | 六動詞接到「在用的」那些上游 | 不是 whisper／ollama |

帳號上其他 repo（agent 護欄、LINE、網站、runtime）屬另一條線，人沒問不展開。
